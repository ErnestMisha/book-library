{
  "name": "book-library",
  "version": "1.0.0",
  "description": "app for managing books in library",
  "scripts": {
    "dev": "pnpm --stream -r dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.8",
    "eslint": "^8.49.0",
    "husky": "^9.0.10",
    "prettier": "^3.0.3",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  },
  "engines": {
    "node": "^20.0.0",
    "pnpm": "^8.0.0"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
